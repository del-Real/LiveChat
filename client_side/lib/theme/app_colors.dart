import 'package:flutter/material.dart';

// FILE INTENTION: Store all brand-specific and common application colors in one place

// Primary branding color (Purple: used for buttons, my messages, and logo accents)
const Color AppPrimaryColor = Color(0xFF8A2BE2); 

// The dark background color used across the app (Scaffold background)
const Color AppDarkBackground = Color(0xFF121212);

// Standard background color for non-me chat bubbles (Light Gray)
const Color AppForeignBubbleColor = Color(0xFFE0E0E0); 

// Accent color for success/connection indicators
const Color AppSuccessColor = Colors.greenAccent; 

// Accent color for error/disconnection indicators
const Color AppErrorColor = Colors.redAccent;